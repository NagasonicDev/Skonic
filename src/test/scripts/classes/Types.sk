test "skin type":
    set {_s} to skin with value "abc" and signature "def"
    assert type of {_s} is "skin" with "was wrong"

test "chatcolor type":
    set {_c} to red
    assert type of {_c} is "chatcolor" with "was wrong"
