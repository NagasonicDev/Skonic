test "citizen is leashable":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    assert npc {npc::1} is leashable with "was wrong"
    delete all npcs
