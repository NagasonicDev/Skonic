test "citizen pathfinding range":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen pathfinding range of {npc::1} to 50
    assert citizen pathfinding range of {npc::1} is 50 with "was wrong"
    delete all npcs
