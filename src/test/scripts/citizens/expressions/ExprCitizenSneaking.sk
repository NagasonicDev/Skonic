test "citizen sneaking":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen sneaking of {npc::1} to true
    assert citizen sneaking of {npc::1} is true with "was wrong"
    delete all npcs
