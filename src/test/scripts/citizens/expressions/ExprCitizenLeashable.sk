test "citizen leashable expression":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen leashable of npc {npc::1} to false
    assert citizen leashable of npc {npc::1} is false with "was wrong"
    delete all citizens
