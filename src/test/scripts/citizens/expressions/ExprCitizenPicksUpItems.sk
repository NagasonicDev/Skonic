test "citizen picks up items expression":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen picks up items of npc {npc::1} to true
    assert citizen picks up items of npc {npc::1} is true with "was wrong"
    delete all citizens
