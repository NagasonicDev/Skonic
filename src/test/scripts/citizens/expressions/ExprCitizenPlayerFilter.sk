test "citizen player filter":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen player filter of {npc::1} to true
    assert citizen player filter of {npc::1} is true with "was wrong"
    delete all npcs
