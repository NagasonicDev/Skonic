test "citizen player filter":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set player filter of npc {npc::1} to true
    assert player filter of npc {npc::1} is true with "was wrong"
    delete all citizens
