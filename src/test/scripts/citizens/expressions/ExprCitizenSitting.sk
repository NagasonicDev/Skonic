test "citizen sitting":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen sitting of npc {npc::1} to true
    assert citizen sitting of npc {npc::1} is true with "was wrong"
    delete all citizens
