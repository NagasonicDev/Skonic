test "citizen gamemode":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen gamemode of {npc::1} to creative
    assert citizen gamemode of {npc::1} is creative with "was wrong"
    delete all npcs
