test "citizen entity type":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set entity type of npc {npc::1} to zombie
    assert entity type of npc {npc::1} is zombie with "was wrong"
    delete all citizens
