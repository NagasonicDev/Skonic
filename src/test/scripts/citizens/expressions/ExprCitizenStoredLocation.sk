test "citizen stored location":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set {_l} to citizen stored location of {npc::1}
    assert {_l} is set with "was wrong"
    delete all npcs
