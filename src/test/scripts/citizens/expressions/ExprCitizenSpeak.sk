test "citizen speech trait":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen speech trait of {npc::1} to true
    assert citizen speech of {npc::1} is true with "was wrong"
    delete all npcs
