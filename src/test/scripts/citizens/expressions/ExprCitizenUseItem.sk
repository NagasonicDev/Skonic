test "citizen using held item":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set using held item of npc {npc::1} to true
    assert using held item of npc {npc::1} is true with "was wrong"
    delete all citizens
