test "citizen using held item":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen using held item of {npc::1} to true
    assert citizen using held item of {npc::1} is true with "was wrong"
    delete all npcs
