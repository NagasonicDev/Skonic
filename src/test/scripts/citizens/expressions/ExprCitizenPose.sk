test "citizen entity pose":
    spawn zombie citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen entity pose of {npc::1} to crouch
    assert citizen entity pose of {npc::1} is crouch with "was wrong"
    delete all npcs
