test "citizen gravity":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set gravity of npc {npc::1} to false
    assert gravity of npc {npc::1} is false with "was wrong"
    delete all citizens
