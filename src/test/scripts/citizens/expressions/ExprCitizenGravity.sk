test "citizen gravity":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen gravity of {npc::1} to false
    assert citizen gravity of {npc::1} is false with "was wrong"
    delete all npcs
