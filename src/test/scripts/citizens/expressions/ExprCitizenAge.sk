test "citizen age":
    spawn zombie citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set age of npc {npc::1} to 0
    assert age of npc {npc::1} is 0 with "was wrong"
    delete all citizens
