test "citizen age":
    spawn zombie citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen age of {npc::1} to 0
    assert citizen age of {npc::1} is 0 with "was wrong"
    delete all npcs
