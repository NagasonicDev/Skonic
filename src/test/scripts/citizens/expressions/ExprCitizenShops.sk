test "citizen shop":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set shop trait of npc {npc::1} to true
    assert shop of npc {npc::1} is true with "was wrong"
    delete all citizens
