test "citizen tablist visibility":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set tablist visibility of npc {npc::1} to true
    assert tablist visibility of npc {npc::1} is true with "was wrong"
    delete all citizens
