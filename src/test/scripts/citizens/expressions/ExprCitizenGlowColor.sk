test "citizen glow color":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen glow color of {npc::1} to red
    assert citizen glow color of {npc::1} is red with "was wrong"
    delete all npcs
