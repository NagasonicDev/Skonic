test "activation range":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set activation range of npc {npc::1} to 32
    assert activation range of npc {npc::1} is 32 with "was wrong"
    delete all citizens
