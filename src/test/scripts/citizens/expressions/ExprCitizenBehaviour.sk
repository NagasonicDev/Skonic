test "citizen behaviour":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set behaviour of npc {npc::1} to true
    assert behaviour of npc {npc::1} is true with "was wrong"
    delete all citizens
