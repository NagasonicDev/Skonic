test "citizen move destination":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    assert citizen move destination of {npc::1} is not set with "was wrong"
    clear {npc::1}
