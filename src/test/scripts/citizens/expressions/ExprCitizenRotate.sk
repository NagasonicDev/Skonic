test "citizen rotation":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set rotation of npc {npc::1} to 90
    assert rotation of npc {npc::1} is set with "was wrong"
    delete all citizens
