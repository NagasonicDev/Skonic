test "citizen name tag visibility":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set name tag visibility of npc {npc::1} to false
    assert name tag visibility of npc {npc::1} is false with "was wrong"
    delete all citizens
