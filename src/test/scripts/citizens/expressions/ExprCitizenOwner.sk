test "citizen owner":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen owner of {npc::1} to player's uuid
    assert citizen owner of {npc::1} is player's uuid with "was wrong"
    delete all npcs
