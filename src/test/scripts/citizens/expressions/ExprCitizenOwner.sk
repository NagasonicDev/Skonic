test "citizen owner":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set {uuid} to random uuid
    set citizen owner of {npc::1} to {uuid}
    assert citizen owner of {npc::1} is {uuid} with "was wrong"
    delete all npcs
