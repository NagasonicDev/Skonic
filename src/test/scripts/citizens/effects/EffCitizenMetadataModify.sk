test "modify citizen metadata":
    spawn citizen named "test" at spawn of world "world"
    set {npc::1} to last created citizen
    set citizen activation_range metadata of {npc::1} to 64
    assert true is true with "was wrong"
    delete all npcs
