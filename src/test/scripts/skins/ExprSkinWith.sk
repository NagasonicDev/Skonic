test "skin with value and signature":
    set {_s} to skin with value "abc" and signature "def"
    assert texture value of {_s} is "abc" with "was wrong"
    assert skin signature of {_s} is "def" with "was wrong"
